import { createSlice } from "@reduxjs/toolkit"

const initialState = {
    wall: 0,
}

const systemAdditionsSlice = createSlice({
    name: "systemAdditions",
    initialState,
    reducers: {
        
    }
})